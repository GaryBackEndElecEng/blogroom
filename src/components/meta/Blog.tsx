import type { Metadata } from 'next';

export const metaRegister: Metadata = {
    title: " The Blog Room -Registration page",
    description: "It allows free blogging. All you need is an email and a name."
}